class SO100Driver():
    def __init__(self, name, config_path):
        self.name = name
        self.config_path = config_path
        print(f"SO100Driver initialized with name: {name} and config_path: {config_path}")
