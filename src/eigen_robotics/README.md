eigen_robotics is the real package that gets built and uploaded. Install tools expect that name, so it has to stay.

eigen is a shortcut that just points to the same code so you can write import eigen. Think of it as a friendly nickname.